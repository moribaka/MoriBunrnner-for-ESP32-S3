## GBA Intel Alignment TODO

- [ ] Stop GBA write flow when CFI cannot be read. Do not continue with ID-table geometry for write/erase/program.
- [ ] Keep protocol selection strict: identify NOR by ID, cache the command set, and only run that exact protocol.
- [ ] Align Intel CFI entry/reset flow with ChisLink:
  - Intel reset with `0x50`, `0xFF`
  - Try CFI enter at `0xAA <- 0x98`
  - Retry at `0x00 <- 0x98` if needed
  - Reset back to read mode with Intel reset, not AMD `0xF0`
- [ ] Add a dedicated Intel ID read path (`0x50`, `0xFF`, `0x90`) instead of relying only on AMD unlock ID reads.
- [ ] Change GBA write scheduling to sector-boundary erase/write, modeled after ChisLink:
  - Erase when programming reaches a sector boundary
  - Optionally pre-erase the next sector
  - Avoid large upfront erase windows for Intel carts
- [ ] Keep command/data swap handling explicit:
  - command/status words translated for D0/D1 swap
  - payload data written raw
- [ ] Clean GBA log wording so there is no more "fallback" language in the write path.
- [ ] Re-test Intel `0x89 00 B0 88 04 00 89 00` cart:
  - ID read
  - CFI read
  - first-sector erase
  - buffered program
